# Python program to insert a given element at the end
# of an array using built-in methods
 
arr = [10, 20, 30, 40]
element = 50
 
# Use append() to add element at the end
arr.append(element)
print("After inserting at the end:")
print(arr)# Python program to insert a given element at the end
# of an array using built-in methods
 
arr = [10, 20, 30, 40]
element = 50
 
# Use append() to add element at the end
arr.append(element)
print("After inserting at the end:")
print(arr)